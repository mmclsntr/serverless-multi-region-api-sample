{
    "regions": {
        "primary_region": "${PRIMARY_REGION}",
        "secondary_region": "${SECONDARY_REGION}"
    },
    "api_name": "${APP_NAME}",
    "stage": "${STAGE}",
    "acm_certificate": {
        "primary_region": "${acm_certificate_primary}",
        "secondary_region": "${acm_certificate_secondary}"
    },
    "zone_id":  "${zone_id}",
    "domain_name": "${domain_name}",
    "api_key": "${api_key}",
    "api_stages": ${API_STAGE_LIST}
}
